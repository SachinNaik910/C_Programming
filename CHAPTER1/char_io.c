/*Athour: Sachin Naik
  Character print*/

#include <stdio.h>

main()
{
    char c,ch;
    c = getchar();
    putchar(c);
    printf("\n");
    /*ch = getch(); // *char() is std func but *ch() us non std func
    putch(ch);*/
    return 0;
}
